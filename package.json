{
  "name": "babellog-api",
  "version": "1.0.0",
  "description": "Serveur API pour les examens",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
